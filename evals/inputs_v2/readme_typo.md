# README 片段（要改的就这一处）

## 待改内容

下面这段 README 里有几个错别字 / 大小写问题，帮我改一下就行：

```
## Instalation

To run the projet, frist install dependencies:

    npm intall

Then start the dev server with `npm run dev`. The app will be avaliable at http://localhost:3000.
```

就是把 `Instalation`→`Installation`、`projet`→`project`、`frist`→`first`、`npm intall`→`npm install`、`avaliable`→`available` 改对。没别的了。
